set autoindent
set number
set mouse=a

" size of a hard tabstop
set tabstop=4

" size of an "indent"
set shiftwidth=4

" use spaces instead of tabs
set softtabstop=4
set smarttab
set expandtab

color desert

" map // to search for what is visually selected
vnoremap // y/<C-R>"<CR>
